import React from 'react';
import { storiesOf } from '@storybook/react';

export default (root) => {
  storiesOf('Flash Cards', root).add('Flash Card', () => (
    <div>
flash card
    </div>
  ));
};
